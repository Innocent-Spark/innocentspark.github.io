---
layout: base.njk
title: Portuguese Riverside Co-Living Project
bodyClass: "bg-gray-100 text-gray-900"
externalStyles:
  - https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css
pageScripts: []
customStyles: |
    /* Styles for the updates section */
    .update-entry {
        border-left: 4px solid #16a34a;
        padding-left: 1rem;
        margin-bottom: 2rem;
    }
    
    .update-date {
        font-weight: 600;
        color: #16a34a;
        margin-bottom: 0.5rem;
    }
    
    /* Ensure proper paragraph formatting */
    section p {
        margin-bottom: 1rem;
    }
    
    section h1 {
        font-size: 1.875rem;
        font-weight: 700;
        margin-bottom: 1rem;
    }
    
    section h2 {
        font-size: 1.5rem;
        font-weight: 600;
        margin-bottom: 0.75rem;
        margin-top: 1.5rem;
    }
    
    /* Fix bullet points display */
    section ul {
        list-style-type: disc !important;
        margin-left: 2rem !important;
        margin-bottom: 1.5rem !important;
        padding-left: 1rem !important;
    }
    
    section ol {
        list-style-type: decimal !important;
        margin-left: 2rem !important;
        margin-bottom: 1.5rem !important;
        padding-left: 1rem !important;
    }
    
    section li {
        margin-bottom: 0.5rem !important;
        display: list-item !important;
        list-style-position: outside !important;
    }
    
    section a {
        color: #16a34a;
        text-decoration: underline;
    }
    
    .home-link {
        display: inline-block;
        margin-bottom: 1rem;
        color: #16a34a;
        text-decoration: underline;
        font-weight: 600;
    }

    .tab-nav {
        display: flex;
        gap: 0;
        border-bottom: 2px solid rgba(255,255,255,0.3);
        margin-top: 1rem;
    }

    .tab-nav a {
        padding: 0.75rem 1.5rem;
        color: rgba(255,255,255,0.8);
        text-decoration: none;
        font-weight: 500;
        border-bottom: 2px solid transparent;
        margin-bottom: -2px;
        transition: all 0.2s;
    }

    .tab-nav a:hover {
        color: white;
        background: rgba(255,255,255,0.1);
    }

    .tab-nav a.active {
        color: white;
        border-bottom-color: white;
        font-weight: 600;
    }

    /* Accordion styles */
    details {
        border: 1px solid #e5e7eb;
        border-radius: 0.5rem;
        padding: 1rem;
        margin-bottom: 1rem;
        background: white;
    }

    details[open] {
        border-color: #16a34a;
    }

    summary {
        cursor: pointer;
        font-weight: 600;
        font-size: 1.125rem;
        color: #16a34a;
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 0.5rem 0;
    }

    summary:hover {
        color: #15803d;
    }

    .house-count {
        font-size: 0.875rem;
        color: #6b7280;
        font-weight: normal;
    }

    .house-gallery {
        margin-top: 1rem;
        padding-top: 1rem;
        border-top: 1px solid #f3f4f6;
    }

    .building-specs {
        border: 1px solid #e5e7eb;
        border-radius: 0.5rem;
        background: #f9fafb;
    }

    .building-specs h4 {
        color: #16a34a;
        border-bottom: 2px solid #16a34a;
        padding-bottom: 0.5rem;
    }

    /* Image Modal Styles */
    .image-modal {
        position: fixed;
        z-index: 9999;
        left: 0;
        top: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0, 0, 0, 0.9);
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .image-modal.hidden {
        display: none;
    }

    .modal-content {
        position: relative;
        max-width: 90%;
        max-height: 90%;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .modal-image {
        max-width: 100%;
        max-height: 90vh;
        object-fit: contain;
    }

    .modal-close {
        position: absolute;
        top: -40px;
        right: 0;
        color: #fff;
        font-size: 40px;
        font-weight: bold;
        cursor: pointer;
        transition: color 0.3s;
    }

    .modal-close:hover {
        color: #16a34a;
    }

    /* Make images clickable */
    .gallery-image {
        cursor: pointer;
        transition: transform 0.2s, opacity 0.2s;
    }

    .gallery-image:hover {
        transform: scale(1.05);
        opacity: 0.9;
    }

---


<header class="bg-green-600 text-white py-6">
    <div class="container mx-auto px-4">
        <h1 class="text-3xl font-bold">{{ portugal.meta.title }}</h1>
        <p class="mt-2">{{ portugal.meta.headerText | markdown | safe }}</p>
        <nav class="tab-nav">
            <a href="/portugal/" class="active">Overview</a>
            <a href="/portugal/values/">Values</a>
        </nav>
    </div>
</header>

<main class="container mx-auto px-4 py-8">
    {% for section in portugal.meta.sections %}
        {% set content = portugal[section] %}
        <section class="mb-8">
            {{ content | safe }}
        </section>
    {% endfor %}
    
    <section class="mb-8">
        <h2 class="text-2xl font-bold mb-4">Gallery</h2>

        {% if media.houses.length > 0 %}
        <div class="mb-6">
            <h3 class="text-xl font-semibold mb-3">Houses</h3>
            {% for house in media.houses %}
            <details>
                <summary>
                    <span>🏠 {{ house.name }}</span>
                    <span class="house-count">{{ house.count }} photos</span>
                </summary>
                <div class="house-gallery">
                    {% if house.buildingSpecs %}
                    <div class="building-specs mb-6 p-4 bg-gray-50 rounded-lg">
                        <h4 class="text-lg font-semibold mb-3 text-green-600">Building Specifications</h4>
                        
                        <div class="space-y-2">
                            <p><strong>Building footprint:</strong> {{ house.buildingSpecs.footprint }} m²</p>
                            
                            {% if house.buildingSpecs.grossBuiltArea %}
                            <p><strong>Gross built area:</strong> {{ house.buildingSpecs.grossBuiltArea }} m²</p>
                            {% endif %}
                            
                            {% if house.buildingSpecs.grossBuiltAreaWithExtra %}
                            <p><strong>Gross built area with extra 30%:</strong> {{ house.buildingSpecs.grossBuiltAreaWithExtra }} m²</p>
                            {% endif %}
                            
                            {% if house.buildingSpecs.grossBuiltArea1Floor %}
                            <p><strong>Gross built area option 1 floor:</strong> {{ house.buildingSpecs.grossBuiltArea1Floor }} m²</p>
                            {% endif %}
                            
                            {% if house.buildingSpecs.grossBuiltArea1FloorWithExtra %}
                            <p><strong>Gross built area with extra 30%:</strong> {{ house.buildingSpecs.grossBuiltArea1FloorWithExtra }} m²</p>
                            {% endif %}
                            
                            {% if house.buildingSpecs.grossBuiltArea2Floors %}
                            <p><strong>Gross built area option 2 floors:</strong> {{ house.buildingSpecs.grossBuiltArea2Floors }} m²</p>
                            {% endif %}
                            
                            {% if house.buildingSpecs.grossBuiltArea2FloorsWithExtra %}
                            <p><strong>Gross built area with extra 30%:</strong> {{ house.buildingSpecs.grossBuiltArea2FloorsWithExtra }} m²</p>
                            {% endif %}
                        </div>
                    </div>
                    {% endif %}
                    
                    <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
                        {% for image in house.images %}
                        <img src="{{ image.path }}" alt="{{ image.alt }}" class="w-full h-auto rounded cursor-pointer hover:opacity-90 transition-opacity gallery-image" loading="lazy">
                        {% endfor %}
                    </div>
                </div>
            </details>
            {% endfor %}
        </div>
        {% endif %}

        {% if media.images.length > 0 %}
        <div>
            <h3 class="text-xl font-semibold mb-3">General Photos</h3>
            <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
                {% for image in media.images %}
                <img src="{{ image.path }}" alt="{{ image.alt }}" class="w-full h-auto rounded cursor-pointer hover:opacity-90 transition-opacity gallery-image" loading="lazy">
                {% endfor %}
            </div>
        </div>
        {% endif %}

        {% if media.videos.length > 0 %}
        <div class="mt-6">
            <h3 class="text-xl font-semibold mb-3">Videos</h3>
            <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
                {% for video in media.videos %}
                <video src="{{ video.path }}" controls class="w-full h-auto rounded" preload="none"></video>
                {% endfor %}
            </div>
        </div>
        {% endif %}
    </section>

    <section class="mb-8">
        <h2 class="text-2xl font-bold mb-4">Location</h2>
        <p class="mb-4">Come and visit!:</p>
        <div class="aspect-w-16 aspect-h-9">
            <iframe src="{{ portugal.meta.mapEmbedUrl }}" width="100%" height="480" frameborder="0" style="border:0;" allowfullscreen="" aria-hidden="false" tabindex="0"></iframe>
        </div>
    </section>
</main>

<!-- Image Modal -->
<div id="imageModal" class="image-modal hidden">
    <div class="modal-content">
        <span class="modal-close">&times;</span>
        <img class="modal-image" id="modalImage" src="" alt="">
    </div>
</div>

<footer class="bg-gray-800 text-white py-4">
    <div class="container mx-auto px-4 text-center">
        <!-- <p>&copy; 2024 Portuguese Riverside Co-Living Project. All Rights Reserved.</p> -->
    </div>
</footer>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const modal = document.getElementById('imageModal');
    const modalImage = document.getElementById('modalImage');
    const closeButton = document.querySelector('.modal-close');
    
    // Add click event to all gallery images
    const galleryImages = document.querySelectorAll('.gallery-image');
    
    galleryImages.forEach(img => {
        img.addEventListener('click', function() {
            modal.classList.remove('hidden');
            modalImage.src = this.src;
            modalImage.alt = this.alt;
        });
    });
    
    // Close modal when clicking the X
    closeButton.addEventListener('click', function() {
        modal.classList.add('hidden');
    });
    
    // Close modal when clicking outside the image
    modal.addEventListener('click', function(e) {
        if (e.target === modal || e.target === modal.querySelector('.modal-content')) {
            modal.classList.add('hidden');
        }
    });
    
    // Close modal with Escape key
    document.addEventListener('keydown', function(e) {
        if (e.key === 'Escape' && !modal.classList.contains('hidden')) {
            modal.classList.add('hidden');
        }
    });
});
</script> 